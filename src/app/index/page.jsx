import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Hub Attualize',
  description: 'o sistema da attualize contabil',
};

export default function Page() {
  return <HomeView />;
}
